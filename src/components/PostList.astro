---
import PostCard from './PostCard.astro';

interface Props {
  posts: any[];
  showImages?: boolean;
  showExcerpt?: boolean;
  showSeparators?: boolean;
  showTags?: boolean;
  title?: string;
}

const { 
  posts, 
  showImages = true, 
  showExcerpt = true, 
  showSeparators = false, 
  showTags = true,
  title 
} = Astro.props;
---

<section aria-labelledby={title ? 'post-list-title' : undefined}>
  {title && (
    <h2 id="post-list-title" class="text-2xl font-semibold mb-6">{title}</h2>
  )}
  
  <div class="border-b border-gray-200 dark:border-gray-800" role="feed" aria-busy="false">
    {posts.length === 0 ? (
      <p class="py-8 text-gray-500 text-center">未找到文章。</p>
    ) : (
      posts.map((post, index) => (
        <PostCard 
          post={post} 
          showImage={showImages} 
          showExcerpt={showExcerpt} 
          showTags={showTags} 
        />
      ))
    )}
  </div>
</section>
